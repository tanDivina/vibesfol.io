// Simplified login configuration without the problematic auth UI components
export const oauthProviders = [] // Disabled for now to avoid compatibility issues

export const sharedAppearance = {
  // Removed auth UI specific configuration
}
